name: Schedule Run

on:
  schedule:
    - cron: "42 11 1 9 *"

jobs:
  schedule-task:
    runs-on: ubuntu-latest
    steps:
      - name: "Checking schedule run"
        run: echo "Schedule running successfully at 11:42"
